<pre>
  <strong>Title:</strong> <%= @project.title %>
  <strong>Description:</strong> <%= @project.text %>
</pre>

<%- unless @project.sources_ordered.empty? %>
<pre>
  <strong>Sources:</strong>
<% @project.sources_ordered.each do |item| %>
    <%= link_to item.url , item.url, title: "SCM TYPE: #{item.scm_type}" %>
<% end %>
</pre>
<%- end %>

<%- unless @project.builds.empty? %>
<ol>
    <strong>Builds:</strong>
    <% @project.builds.reverse.each do |item| %>
        <li>
        <%= link_to "ID: #{item.id}", [@project, item] %> -  <%= item[:state] %> |
        <%= link_to "delete", [@project, item], :method => :delete %> 
        </li>
    <% end %>
</ol>
<% end %>

[ 

 <%= link_to 'Edit Project', edit_project_path(@project) %> | 
 <%= link_to 'Build Project', [@project, @project.builds.build], method: :post  %> |
 <%= link_to 'Delete', project_path(@project), method: :delete, data: { confirm:    "You are about to delete project # #{@project[:id]}. Are you shure ?" }  %>

]

